const ONE = 1;
const TWO = 2;

/*
    名前付きエクスポート
      - export は 外部から使用できるように変数や関数、クラスを使えるようにするためのもの
      - 「export { ONE, TWO };」 にすることでまとめてエクスポートもできる
      - 「export const TEN = 10;」のように定義と同時にエクスポートもできる
 */
export { ONE, TWO };
export const TEN = 10;
